import React from "react";
import {
  AboutMeText,
  AboutWrapper,
} from "../components/AboutSection/AboutElements";

const NotFound = () => {
  return <AboutWrapper id="about">Page not found</AboutWrapper>;
};

export default NotFound;
